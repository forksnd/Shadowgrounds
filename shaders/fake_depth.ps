
sampler samDiffuse : register(s0);

struct PS_IN
{
    float4 color : COLOR;
    float2 uv    : TEXCOORD0;
};

float4 main(PS_IN ps_in):COLOR
{
    float4 color = ps_in.color;

    color.a = tex2D(samDiffuse, ps_in.uv).a;

    return color;
}